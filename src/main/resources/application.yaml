spring:
  application:
    name: Practice_JPA_Specification
  server:
    port: 8080
  datasource:
    url: jdbc:postgresql://localhost:5432/practice-jpa-specification
    username: anant-saini
    password: HarryPotter@123
    driver-class-name: org.postgresql.Driver

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true

  springdoc:
    swagger-ui:
      path: /swagger-ui.html
    api-docs:
      path: /v3/api-docs
      version: openapi_3_1